LittleFS runtime data are stored in this partition by the firmware.

czech_border_300.geojson is a simplified CZE ADM0 boundary derived from the
geoBoundaries dataset. The firmware itself uses the matching 300-point C++
array in src/map_renderer.cpp.
